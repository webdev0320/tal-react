import Home from '../src/Home.jsx';

export const metadata = {
  title: 'Taxaccolega — Home',
  description: 'Accountancy, bookkeeping and taxation services for small businesses.'
};

export default function Page() {
  return <Home />;
}
