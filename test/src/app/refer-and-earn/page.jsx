import { metadataForPath } from '../../lib/seo';
import ReferAndEarn from '../../views/ReferAndEarn';

export const metadata = metadataForPath('/refer-and-earn');

export default function Page() {
  return <ReferAndEarn />;
}
