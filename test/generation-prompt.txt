You are an expert Senior Full-Stack React.js Developer. Your task is to generate a pristine, production-ready React component based on raw unstructured text migrated from a legacy WordPress page.

### Objectives:
1. Re-architect the unstructured text into a clean, modern, and semantic JSX structure using proper HTML tags (<main>, <section>, <h1>-<h6>, <p>, <ul>, <li>, <blockquote>).
2. Maintain absolute data and text integrity—do not alter, summarize, or truncate any sentences, descriptions, or headings from the source data.
3. Use a sleek, modern layout structure. Ensure the design follows a responsive structural hierarchy suitable for clean styling (e.g., semantic semantic grouping containers).

### Technical Rules:
- Return ONLY the executable code block for the component. No conversational text, introductory pleasantries, or setup markdown explanations.
- Use standard functional components with modern ES6 arrow syntax.
- Ensure proper escaping for special characters inside JSX text (like straight quotes or curly braces) using native entities or simple string wraps.

### Source Inputs:
---
[PAGE SLUG/NAME]:
{{SLUG_NAME}}

[RAW WORDPRESS CONTENT]:
{{WP_CONTENT}}
---

### Expected Output Structure:
import React from 'react';

const ComponentName = () => {
  return (
    <main className="page-wrapper">
      {/* Structural sections and complete textual content here */}
    </main>
  );
};

export default ComponentName;